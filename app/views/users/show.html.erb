
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/user_show', user: @user %>

      <%= render 'books/book_new', book: @book %>
    </div>
    <div class="col-md-8 offset-md-1 pt-md-3">
       <%= render 'books/book_index', books: @user.books %>
    </div>
  </div>

