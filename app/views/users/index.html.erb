<h1>Users</h1>
<table>
  <head>
    <tr>
      <th>image</th>
      <th>name</th>
    </tr>
  </head>
  <tbody>
    <% @users.each do |user| %>
    <tr>
      <td><%= attachment_image_tag @user, :image %></td>
      <td><%= user.name %></td>
      <td><%= link_to "Show" %></td>
    </tr>
    <% end %>
  </tbody>
</table>