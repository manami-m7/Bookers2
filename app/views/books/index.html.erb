
 <div class="container">
   <% if @book.errors.any? %>
      <h3 class="error-color"</h3><%= @book.errors.count %>error prohibited this book from being saved:</h3><br>
       <% @book.errors.full_messages.each do |message| %>
         <ul>
             <li class="error-color"><%= message %></li>
         </ul>
      <% end %>
    <% end %>
    <div class="row">
    <div class="col-md-3">
       <%= render 'users/user_show', user: @user %>
       <%= render 'books/book_new', book: @book %>
    </div>

    <div class="col-md-8 offset-md-1 pt-3">
        <%= render 'search/search_form' %>
        <p></p>
        <%= render 'books/book_index', books: @books %>
    </div>
  </div>
